#include <stdio.h> 
#include <stdlib.h>

/*
void dengyao(int m);
int main(void)
{
	dengyao(10);

	system("pause");
	return 0;
}

void dengyao(int m)//画图，找空格与 * 和行数的关系
{
	int i, j;
	int n = 1;
	for ( n = 1; n <= m; n++)
	{
		for (i = m-n; i > 0; i--)
		{
			putchar(' ');
		}
		for (j = 0; j < 2 * n - 1; j++)
		{
			putchar('*');
		}
		putchar('\n');
	}
}
*/